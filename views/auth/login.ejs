<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link
    href="https://fonts.googleapis.com/css2?family=Barlow:wght@400;500;600;700&display=swap"
    rel="stylesheet"
  />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <link href="https://maxcdn.bootstrapcdn.com/bootstrap/5.1.3/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/styles.css">
  <link rel="stylesheet" href="/css/auth.css">
  <title>Login - Ecommerce Outlaws</title>
</head>
<body>

</head>
<body>

<!-- Animated Background -->
<div class="background-animation">
  <div class="floating-shape"></div>
  <div class="floating-shape"></div>
  <div class="floating-shape"></div>
  <div class="floating-shape"></div>
  <div class="floating-shape"></div>
  <div class="gradient-orb"></div>
  <div class="gradient-orb"></div>
</div>

<div class="auth-container">
  <a href="/" class="brand-logo d-none d-md-block">
    <i class="fas fa-bolt"></i>Outlaws
  </a>

  <div class="auth-card">

    <!-- Mobile brand logo -->
    <div class="d-block d-md-none">
      <a href="/" class="brand-logo">
        <i class="fas fa-bolt"></i>Outlaws
      </a>
    </div>

    <div class="brand-section">
      <div class="brand-title">
        <i class="fas fa-sign-in-alt"></i>
        <span>Welcome Back</span>
      </div>
      <p class="brand-subtitle">Sign in to your account and continue shopping</p>
      <div class="divider"></div>
    </div>

    <% if (errors && errors.general) { %>
      <div class="custom-auth-error mb-4">
        <i class="fas fa-exclamation-triangle me-2"></i>
        <%= errors.general %>
      </div>
    <% } %>

    <form action="/login" method="POST">
      <div class="form-group">
        <i class="fas fa-envelope input-icon"></i>
        <input 
          type="email" 
          id="email" 
          name="email" 
          class="input-field" 
          placeholder="Email Address" 
          value="<%= oldInput?.email || '' %>" 
          required
        >
        <% if (errors && errors.email) { %>
          <small class="error-msg"><%= errors.email %></small>
        <% } %>
      </div>

      <div class="form-group">
        <i class="fas fa-lock input-icon"></i>
        <input 
          type="password" 
          id="password" 
          name="password" 
          class="input-field" 
          placeholder="Password" 
          required
        >
        <% if (errors && errors.password) { %>
          <small class="error-msg"><%= errors.password %></small>
        <% } %>
      </div>

      <button type="submit" class="btn-primary">
        <i class="fas fa-sign-in-alt"></i>
        Sign In
      </button>
    </form>

    <div class="auth-footer">
      <p>Don't have an account? <a href="/register">Register now</a></p>
    </div>
  </div>
</div>

</body>
</html>